[project]
name = "arima-executive-os"
version = "0.1.0"
description = ""
authors = []
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings",
    "SQLAlchemy[asyncio]>=2.0",
    "asyncpg",
    "aiosqlite",
    "alembic",
]
[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
    "ruff",
    "mypy",
]
[build-system]
requires = ["setuptools", "wheel"]
