[project]
name = "arima-executive-os"
version = "0.1.0"
description = ""
authors = []
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings",
    "SQLAlchemy[asyncio]>=2.0",
    "asyncpg",
    "aiosqlite",
    "alembic",
    "pwdlib[argon2]>=0.3",
    "PyJWT>=2.10",
    "email-validator>=2.2",
    "python-multipart>=0.0.20",
]
[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
    "ruff",
    "mypy",
]
[build-system]
requires = ["setuptools", "wheel"]
